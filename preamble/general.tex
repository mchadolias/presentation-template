% Encoding & fonts
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}

% Graphics & figures
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

% Tables
\usepackage{booktabs}
\usepackage{multirow}

% Code listings (minted requires -shell-escape)
\usepackage{minted}
\setminted{fontsize=\footnotesize, breaklines, frame=single}

% Hyperlinks
\usepackage{hyperref}
\hypersetup{
  pdfpagemode=UseNone, % no bookmarks panel by default
  pdfstartview=FitH,
  pdfhighlight=/O,
  colorlinks=true,
  linkcolor=blue,
  urlcolor=blue,
  citecolor=blue,
}

% Avoid duplicate destination errors
\pdfstringdefDisableCommands{%
  \def\frametitle#1{#1}%
}